{
  "framework": "nextjs",
  "buildCommand": "npm ci --no-audit --prefer-offline && npm run build",
  "installCommand": "npm ci --no-audit --prefer-offline",
  "crons": [
    {
      "path": "/api/cron/token-refresh",
      "schedule": "0 2 * * *"
    },
    {
      "path": "/api/cron/billing-universal",
      "schedule": "0 9 * * *"
    }
  ]
}
